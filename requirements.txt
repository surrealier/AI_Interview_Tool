# Core desktop runtime uses the Python standard library on Windows.
# Install optional profiles as needed:
#   python -m pip install -r requirements-interactive.txt
#   python -m pip install -r requirements-qwen.txt
